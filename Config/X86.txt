#设备平台
CONFIG_TARGET_x86=y
CONFIG_TARGET_x86_64=y
#设备列表
CONFIG_TARGET_ALL_PROFILES=y
#不集成WIFI驱动
#daed后端所需内核能力
CONFIG_KERNEL_DEBUG_INFO=y
CONFIG_KERNEL_DEBUG_INFO_REDUCED=n
CONFIG_KERNEL_DEBUG_INFO_BTF=y
CONFIG_KERNEL_BPF_EVENTS=y
CONFIG_KERNEL_XDP_SOCKETS=y
CONFIG_DAED_USE_KERNEL_BTF=y
# Use the GitHub Actions host LLVM instead of falling back to /invalid/clang
CONFIG_BPF_TOOLCHAIN_HOST=y
#组件调整
CONFIG_GRUB_IMAGES=y
CONFIG_VMDK_IMAGES=y
CONFIG_TARGET_KERNEL_PARTSIZE=128
CONFIG_TARGET_ROOTFS_PARTSIZE=1024
CONFIG_TARGET_ROOTFS_EXT4FS=n
#内核模块调整
CONFIG_PACKAGE_kmod-hwmon-w83627ehf=y
CONFIG_PACKAGE_kmod-hwmon-w83627hf=y
CONFIG_PACKAGE_kmod-w83627hf-wdt=y

#网络优化
#通过内核包选择 BBR，避免混用未经确认的 CONFIG_KERNEL_* 符号
CONFIG_PACKAGE_kmod-tcp-bbr=y
#fq 调度器（sch_fq 模块，配合 BBR 公平队列；default_qdisc=fq 依赖它）
CONFIG_PACKAGE_kmod-sched=y
#完整 tc 工具，用于检查 fq 和各接口队列
CONFIG_PACKAGE_tc-full=y

#PassWall 使用官方新版 Xray 核心；软件包仍由 APK 管理。
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray=y
CONFIG_PACKAGE_xray-core=y
